= f.label "Session Date"
= f.text_field :session_date
= f.label "student"
= f.select :student, options_for_select(select_options(@students, :name))
= f.label 'duration'
= f.text_field :duration
= f.label "notes"
= f.text_field :notes
= f.submit "Save Student"
