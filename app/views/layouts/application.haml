!!!
%html
  %head
    %title TopDashboard
    = stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true
    = javascript_include_tag "application", "data-turbolinks-track" => true
    = csrf_meta_tags
  %body
    = render 'layouts/session_nav'
    - if landing?
      = yield
    - else
      = render 'shared/flash_messages'
      .container
        .row
          .col-md-12
            = yield
