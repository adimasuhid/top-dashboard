%h1 Students

= link_to "add a student", new_student_path

%table#students
  %thead
    %tr
      %th First Name
      %th Last Name
      %th School
      %th Year Level
  %tbody
    - @students.each do |student|
      %tr
        %td= student.first_name
        %td= student.last_name
        %td= student.school
        %td= student.year_level_name
        %td= link_to "edit student", edit_student_path(student)
